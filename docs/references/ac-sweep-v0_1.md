# v0.1 acceptance-criteria sweep (AC1-19)

Task 12.3 of the final block. Spec of record: §14 of
`docs/specs/squall-spec-v0_18-RC.md`. Each row states the
verdict and names the exact evidence — a test (file + function/spec name), a drill (documented,
not executed), or a deferral (with the concrete reason it can't be closed by this repo's code
today).

Per the block's instructions, **AC3 is scored only against the 503 wait contract Squall owns**
(the router-level fallback chain is `router_settings` in the LiteLLM Helm values, outside
squall's v0.1 scope per §14 AC3's own text).

| AC | Verdict | Evidence |
|---|---|---|
| **1** | DEFERRED | Requires real inference end-to-end across Vast/AWS/DigitalOcean. Real-hardware PoC 0/2 are explicitly out of scope for this block. |
| **2** | DEFERRED | Requires a real private-transport deployment (AWS PrivateLink/VPC + marketplace/SSH tunnel) and PoC 5 automation. Real infra, out of scope. |
| **3** | PROVEN BY TEST | `internal/proxy/handler_test.go`: `TestHandler_Asleep_TimesOutWithWaitContract_ZeroBytesBeforeOutcome` (holds, times out, 503+`Retry-After`+state body, nothing written before the outcome) and `TestHandler_UnblocksAndForwardsWhenModelBecomesReadyMidWait` (large hold, forwards once Ready). `internal/proxy/hold_test.go`: `TestAwait_TimesOutStillBlocking`, `TestAwait_UnblocksOnCacheChangeBeforeDeadline`, `TestAwait_CancelledContextTimesOut` prove the generic deadline mechanics `holdTimeout` feeds into. **Caveat:** no test constructs a literal `holdTimeout: 0` CR end-to-end; the "answers immediately" claim for `0` rests on the generic deadline-already-passed path (`Await`'s deadline = `now + holdTimeout`), not a dedicated zero-value test. `test/e2e/e2e_test.go`'s "wakes on a real proxy request and reaches Waking with a live run" spec demonstrates the large-`holdTimeout` half live against a real cluster. |
| **4** | PROVEN BY TEST | Controller side: `internal/controller/squall/model_controller_test.go`'s `TestReconcile_FiftyConcurrentDemandPatches_WakesOnce` (50 concurrent demand patches → exactly one Apply). CAS backstop: `internal/dstack/mock/mock_test.go`'s `TestApply_ConcurrentCallsAreRaceFree`, `TestApply_StaleDeploymentNum_IsRejected`, `TestApply_ForceIsNeverAccepted`, `TestApply_ForceCheckedBeforeCAS`; `internal/dstack/client_test.go`'s `TestClient_Apply_NeverSendsForceOnWire`. Proxy-side coalescing of the burst before it ever reaches the controller: `internal/proxy/demand_test.go`'s `TestDemandCoalescer_ConcurrentSignalsCollapseToOnePatch`, and `internal/proxy/handler_test.go`'s `TestHandler_MaxPendingPerModel_NPlusOnethAnsweredImmediately` (bounds concurrent held requests per model). "Exactly one resulting run/bill" is the CAS guarantee: only one Apply ever wins per generation. |
| **5** | PARTLY PROVEN BY TEST, PARTLY DEFERRED | dstack's fleet idle-release mechanics (F21) are the mock's own behavior, proven directly: `internal/dstack/mock/mock_test.go`'s `TestFleet_FlipReleasesJobNotInstance`, `TestFleet_RewakeResetsIdleClock`. Per **D32** (already logged, `ACCEPTED`), the release itself is invisible on the wire the real `dstack.Client`/CR `status` expose, so `test/e2e/e2e_test.go`'s "stays Asleep on the same run past the fleet's idle window" spec asserts *inertness* (no spurious re-Apply, same run) rather than the release itself — a deliberate, already-documented scope boundary, not a new gap. The literal AC text ("48h without traffic → capacity cost = 0", verified at the instance layer against a real invoice) needs real hardware and real billing — deferred, out of scope. |
| **6** | PROVEN BY TEST | `internal/controller/squall/model_controller_finalizer_envtest_test.go`: `TestReconcile_Delete_DrainFirstTeardown` (drain-first, no mid-stream cut), `TestReconcile_Delete_ReplayAfterAlreadyGone_TreatsNotFoundAsSuccess` (replay-safe), `TestReconcile_Delete_UnreadableProxyEndpoints_NoDestructiveCalls` (partial-read → zero destructive actions). `internal/controller/squall/model_controller_unit_test.go`'s `TestReconcile_UncertainDstackGetError_ReturnsErrorWithoutStatusWrite` covers the API/dstack-unavailability half. No generic instance-age TTL exists anywhere in `phase.go` (confirmed by reading — `Decide` is purely level-triggered on CR + observed state, see `internal/controller/squall/CLAUDE.md`'s own "level-triggered idempotence" note). |
| **7** | DEFERRED (by spec's own design) | §14 AC7's own text calls this "manual at v0.1... Automation is §15" (future work). Not a gap this block introduces or can close; nothing to automate against today. |
| **8** | PARTLY PROVEN BY TEST, PARTLY DEFERRED | The AC19 alert-fires-in-a-test half (explicitly named by AC8's own text) is proven: `config/prometheus/rules_test.go`'s `TestRules_ObservedExceedsDeclaredShape`. Grafana dashboard visibility for infra cost / token usage / derived €-per-1M-tokens / tokens-per-GPU-hour is platform-level (§10: "PromQL `sum_over_time` over the exported price gauge, zero Go code") — no Grafana dashboard JSON/provisioning exists in this repo, and none is required by anything this block touches; deferred as a platform/LGTM-stack concern outside squall's own codebase. |
| **9** | DEFERRED | Real-hardware kill-host drill across three backends. Explicitly out of scope (Tier-1 real-dstack `e2e-local` suite is **D1**, also out of scope for this block). |
| **10** | DEFERRED | Credential scoping, DB encryption, and backup/restore are dstack-server and platform operational concerns, not something `internal/` code implements or a Go test can exercise. Nothing in this repo's scope to verify. |
| **11** | PARTLY PROVEN BY TEST, PARTLY DEFERRED | The workload-eligibility table itself is written down in the spec (§12.3 "Workload eligibility (mandatory classification)"). The CR's `backends:` allowlist field (`api/squall/v1alpha1/model_types.go`'s `Backends []string`) is validated non-empty: `internal/controller/squall/model_validation_test.go`'s `TestValidate_PlacementBackendsEmpty`. **Not enforced at the scheduler level by this repo's Go code**: the FROZEN `dstack.ApplyRequest` (`Name, Replicas, BaseDeploymentNum`) carries no backends/placement/GPU field at all — confirmed by reading `internal/dstack/client.go` — so `Backends` is presence-validated on the CR but never actually forwarded to dstack to constrain instance placement. That wiring is §8 engine-template / run-config work, explicitly out of scope for this block (same boundary as **D28**). |
| **12** | DEFERRED — new gap, logged as **D37** | AC12 requires "immutable CR uid plus naming/tags" so renames can't create ownership ambiguity. `model_controller.go` keys every dstack run by `model.Name` alone (`r.observe(ctx, model.Name, now)`, `dstack.ApplyRequest{Name: model.Name}`); `model.Namespace`/`model.UID` are never read. Confirmed by reading — no test exercises this because there is no code path that uses UID at all. See **D37** for the full writeup and the two possible fixes (both need sign-off: unfreezing `dstack.ApplyRequest` to add a tag field, or a namespaced+UID-derived run name). |
| **13** | PROVEN BY TEST | CAS/no-force backstop: `internal/dstack/client_test.go`'s `TestClient_Apply_NeverSendsForceOnWire`; `internal/dstack/mock/mock_test.go`'s `TestApply_ForceIsNeverAccepted`, `TestApply_ForceCheckedBeforeCAS`, `TestApply_StaleDeploymentNum_IsRejected`. Controller-side crash/replay: `internal/controller/squall/model_controller_test.go`'s `TestReconcile_ConcurrentFlips_LoserFailsLoudlyAndRequeues` (loser never retries with force, fails loudly) and `TestReconcile_StaleCachedStatus_ApplyUsesFreshlyObservedDeploymentNum` (replay after a stale cached status still uses the freshly-observed `deployment_num`, so a crash-then-replay CAS never silently double-applies). |
| **14** | DEFERRED | §8 engine templates (vLLM/llama.cpp runtime-specific behavior isolated to templates) are explicitly out of scope for this block. |
| **15** | DEFERRED — blocked on **D7** | `provisioningTimeout`/`maxLifetime` have no "since-when" anchor in `ModelStatus` or `dstack.Run` (**D7**, `OPEN`); the recommended fix (`status.runStartedAt`) is Task 8.3, explicitly named as out of scope for this block ("blocked on D7"). `internal/metrics/model_age.go`'s own doc comment confirms: "MaxLifetime is explicitly ALERT-ONLY" and references "8.3's DESTRUCTIVE timeout" as the still-missing piece for the `provisioningTimeout`-destroy half. The alert-only half for `maxLifetime` (age gauge, no destroy) is otherwise proven — see AC19. |
| **16** | PROVEN BY TEST | Proxy side (503/404/403 → the request-path signal set): `internal/proxy/decision_test.go`'s `TestDecide` table (`Asleep/Waking/Recreating/Dead → 503 + state`, `Draining → 404 immediately`, `no CR → 404`, `Ready + gateway 403 → 502 auth fault, never forward`) and `TestDecide_UnknownPhase_FailsToward404`; `internal/proxy/handler_test.go`'s `TestHandler_GatewayForbidden_TranslatedTo502WithAlarm`, `TestHandler_NoSuchModel_404Immediately`, `TestHandler_Draining_404ImmediatelyNoBlock`. Controller side (404-equivalent: an uncommanded run death): `internal/controller/squall/phase_test.go`'s `TestDecide` table, "Dead -> recreate + alarm" and "Dead, no demand -> alarm only, no recreate" cases (F20's Asleep/Dead distinction). |
| **17** | PROVEN BY TEST | `internal/controller/squall/model_controller_pin_envtest_test.go`'s `TestReconcile_PinToggle_NoRecreate` (toggling pinned↔on-demand takes effect without a recreate); `internal/controller/squall/model_controller_unit_test.go`'s `TestReconcile_PinnedAwake_DoesNotRequeue`; `internal/controller/squall/phase_test.go`'s table cases "Asleep, pinned, no demand -> still flips", "Dead, pinned, no proxy demand -> still recreates + alarms", and "pinned, replicas up, clean idle window past scaleDownDelaySeconds -> stays Ready, no sleep" (survives idle windows; the `maxLifetime` age alert, not a destructive action, is the only consequence — see AC19/AC15). |
| **18** | DOCUMENTED AS A DRILL — not executed | Task 12.1's `test/e2e/cluster/04-discovery/discovery.yaml` is the CR ("CR merged → model in LiteLLM with the external router profile"). The rest of the chain (revert → drained removal, proxy-outage fail-closed per F31) requires alitellm-operator + its CRDs, which are **not installed** in this repo's kind cluster — confirmed via `kubectl apply -k test/e2e/cluster/04-discovery --dry-run=client` failing with "ensure CRDs are installed first". Installing the sibling operator to close this AC is explicitly out of scope. See `docs/references/phase-12-discovery-seam.md`'s 12.2 for the exact drill steps. "Squall never calls the LiteLLM API" is true by construction today: no LiteLLM API client exists anywhere in this repo's `go.mod`/import graph (**D5** already documents the design: discovery is pull-based, Squall never calls out). |
| **19** | PROVEN BY TEST | `internal/metrics/model_age_test.go`: `TestModelAgeCollector_EmitsPairWhenDeclared`, `TestModelAgeCollector_NoDeclaredMaxLifetime_EmitsNothing`, `TestModelAgeCollector_RunIDChange_ResetsAgeBaseline`, `TestModelAgeCollector_Forget_DropsFromScrape`. `internal/metrics/model_price_test.go`: `TestModelPriceCollector_EmitsPairOnlyWhenBothSidesPresent`, `TestModelPriceCollector_MissingObserved_EmitsNeitherSide`, `TestModelPriceCollector_MissingDeclared_EmitsNeitherSide`; `internal/dstack/probes_test.go`: `TestReplicaPricePerHour_UsesOnlyLiveCurrentDeployment` characterizes the production source. The generic `observed > declared` rule firing "in a test": `config/prometheus/rules_test.go`'s `TestRules_ObservedExceedsDeclaredShape` asserts both gauges are wired as well-formed alert rules. D26 is now closed: live replica provisioning price feeds the observed side when available. |

## Summary

- **Fully proven by test:** AC3, AC4, AC6, AC13, AC16, AC17, AC19 — 7
- **Partly proven by test, partly deferred:** AC5, AC8, AC11 — 3
- **Documented as a drill, not executed:** AC18 — 1
- **Deferred outright:** AC1, AC2, AC7, AC9, AC10, AC12, AC14, AC15 — 8

Total: 19. One genuinely new gap surfaced by this sweep and not previously logged: **AC12 →
D37** (dstack run identity is `model.Name` alone, no UID/tag — see the ledger for the full
writeup). Every other deferral traces to an already-out-of-scope area named in this block's own
instructions (real hardware/PoCs, §8 engine templates, D7-blocked Task 8.3) or an
already-logged, still-`OPEN` ledger item (D5, D28, D32).
