apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: proxy-role
rules:
- apiGroups:
  - squall.ackstorm.ai
  resources:
  - models
  verbs:
  - get
  - list
  - watch
  - patch
